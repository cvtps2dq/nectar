import 'package:flutter/material.dart';

class NeuColors {
  // Backgrounds
  static const Color background = Color(0xFF292D32);
  static const Color surface = Color(0xFF292D32); // Same as BG for neumorphism
  
  // Shadows (Crucial for the 3D effect)
  static const Color shadowLight = Color(0xFF35393F); // Top-Left highlight
  static const Color shadowDark = Color(0xFF1D1F24);  // Bottom-Right shadow
  
  // Text
  static const Color textPrimary = Color(0xFFE0E0E0);
  static const Color textSecondary = Color(0xFFA0A0A0);
  
  // Accents (Nectar Amber)
  static const Color accent = Color(0xFFFFB300);
  static const Color accentPressed = Color(0xFFFF8F00);
  
  // Status
  static const Color red = Color(0xFFE57373);
  static const Color green = Color(0xFF81C784);
}